# QUiZ8OTTO

八个碎片，散落在这些文件的名字里。

每片都带着自己的编号：`c` 与 `k` 各四条，数字 `0 1 2 3` 是它回家的班次。

## 拼合

把 `c` 的 0、1、2、3 号碎片，从每片取第 1 个字、再取第 2 个字……像织布一样**第 i 针落在第 i 号碎片**（`i::4`），四股线交成一条 25 字节的绳。

`k` 的碎片，也照同样的针脚，织成第二条绳。

## 然后

两条绳一样长。把它们**并排、逐字相对**：

> 同则相消归于无，异则相合留其真。
> 字字相照，无进位的相加，落笔处便是谜底。

---
（谜底是一串地址。）
